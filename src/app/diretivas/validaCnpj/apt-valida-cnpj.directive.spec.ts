import { AptValidaCnpjDirective } from './apt-valida-cnpj.directive';

describe('AptValidaCnpjDirective', () => {
  it('should create an instance', () => {
    const directive = new AptValidaCnpjDirective(null);
    expect(directive).toBeTruthy();
  });
});
