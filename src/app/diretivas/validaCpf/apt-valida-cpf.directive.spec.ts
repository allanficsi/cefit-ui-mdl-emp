import { ValidaCpfDirective } from './apt-valida-cpf.directive';

describe('ValidaCpfDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaCpfDirective(null);
    expect(directive).toBeTruthy();
  });
});
