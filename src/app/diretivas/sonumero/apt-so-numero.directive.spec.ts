import { AptSoNumeroDirective } from './apt-so-numero.directive';

describe('AptSoNumeroDirective', () => {
  it('should create an instance', () => {
    const directive = new AptSoNumeroDirective(null);
    expect(directive).toBeTruthy();
  });
});
