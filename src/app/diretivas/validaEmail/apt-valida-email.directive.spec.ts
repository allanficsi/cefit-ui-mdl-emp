import { AptValidaEmailDirective } from './apt-valida-email.directive';

describe('AptValidaEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new AptValidaEmailDirective(null);
    expect(directive).toBeTruthy();
  });
});
